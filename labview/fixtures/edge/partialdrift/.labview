# Written when the service published a host port and had no proxy route, and never
# updated after the port was withdrawn and the route added. Both differences have to be
# reported: the `lan` that is gone, and the `traefik` route that appeared and was never
# written down.
services:
  api:
    description: API published through the tunnel.
    expected:
      ingress: [public, lan]
